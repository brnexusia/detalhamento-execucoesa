name: apply-discard-import
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-discard-import.yml'
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: atacado
          POSTGRES_PASSWORD: atacado
          POSTGRES_DB: atacado_test
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U atacado -d atacado_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://atacado:atacado@127.0.0.1:5432/atacado_test
      PORT: 3000
      BASE_URL: http://127.0.0.1:3000
      SCANNER_WORKER_DISABLED: '1'
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: python .github/scripts/add-discard-import.py
      - run: npm install
      - run: npm run build
      - name: Start app
        run: |
          npm start >/tmp/atacado.log 2>&1 &
          sleep 2
      - run: node server/scanner-discard.integration.mjs
      - name: Commit production change and cleanup helper
        run: |
          rm -f .github/scripts/add-discard-import.py .github/workflows/apply-discard-import.yml
          git config user.name "brnexusia"
          git config user.email "brnexusia@gmail.com"
          git add -A
          git commit -m "Scanner: descartar importações e limpar dados temporários"
          git push origin HEAD:main
